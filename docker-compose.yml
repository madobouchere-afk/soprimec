version: '3.8'

services:
  soprimec:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - soprimec-data:/app/data
      - soprimec-uploads:/app/uploads
    restart: unless-stopped
    environment:
      - NODE_ENV=production

volumes:
  soprimec-data:
  soprimec-uploads:
